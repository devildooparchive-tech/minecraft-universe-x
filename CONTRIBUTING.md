# دليل المساهمة والتطوير

## القواعد الذهبية

1. **اقرأ قبل أن تكتب** — افهم النظام الذي ستعدله ومرّر على `docs/ANALYSIS-v1.md`.
2. **اختبار أولًا** — اكتب اختبار Vitest يفشل، ثم نفّذ الكود حتى ينجح.
3. **Event Bus فقط** — ممنوع استدعاء نظام آخر مباشرة عبر الحدود المعمارية.
4. **JSON للمحتوى** — أي بلوك/شخصية/قدرة جديدة = ملف JSON في `data/`، لا كود.
5. **Commit لكل خطوة** — رسائل بصيغة Conventional Commits:
   - `feat(world): ...` · `fix(core): ...` · `test(ai): ...` · `docs: ...` · `refactor!: ...`

## سير العمل

```bash
git checkout -b feat/<اسم-الميزة>
# ... نفّذ + اختبر
npm test && npm run build
git commit -m "feat(scope): وصف موجز"
```

## معايير قبول الكود

- `tsc --noEmit` بدون أخطاء (strict mode).
- اختبارات الوحدة للمنطق الجديد.
- عدم كسر معيار قبول المرحلة الحالية في SagTask.
- تحديث `docs/sagtask/SAGTASK-MASTER.md` عند إنجاز مهمة.

## للأدوات الذكية (Agents)

- الكود المرجعي في `reference/v1-src/` للقراءة **فقط** — لا تعدله أبدًا.
- الدروس والأخطاء المتكررة تسجل في ذاكرة Hermes الدائمة + `docs/lessons.md`.
